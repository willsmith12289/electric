<li><%= line_item.quantity %> &times; <%= line_item.product.title %> <%= number_to_currency(line_item.total_price) %> </li>
<li><%= link_to 'Delete', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></li>
<hr>